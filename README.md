# dotnet_program

This repository is for testing git submodules.

## Get Submodules

run the following commands within the repo

```bash
git submodule init
git submodule update
```

The submodule will then be downloaded. You can now run the program

```bash
dotnet run project --project .\dotnet_program\dotnet_program.csproj
```
